apiVersion: v1
kind: Pod
metadata:
  name: "minio-test-connection"
  labels:
    app: minio
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['minio-service:{{ .Values.service.port }}']
  restartPolicy: Never
